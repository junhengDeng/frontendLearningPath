// 复杂度是数量级（方便记忆、推广），不是具体数字
// 常见复杂度大小比较：O(n^2) > O(nlogn) > O(n) > O(logn) > O(1)

// 1.时间复杂度

// O(n^2): 2层循环（嵌套循环）
// O(nlogn):快速排序（循环 + 二分）
// O(n): 1层循环
// O(logn): 二分

// 2.空间复杂度

// O(n): 传入一个数组，处理过程生成一个新的数组大小与传入数组一致