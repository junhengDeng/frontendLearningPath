// chartAt 返回指定索引位置处的字符
// concat 拼接字符串，原字符串不变
// indexOf, lastIndexOF
// slice
// substr, substring： 第二参数，substr为截取长度，substring为结束下标
// match：返回一个包含该搜索结果的数组
// replace: 替换
// toLowerCase(), toUpperCase()：转换成小写，大写
// includes(),startsWith(),endsWith()
// repeat()：执行重复次数

